% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_dag_enrich.R
\name{run_dag_enrich}
\alias{run_dag_enrich}
\title{Run enrichment of terms in a DAG ontology}
\usage{
run_dag_enrich(
  obj,
  ont,
  id_col = NULL,
  reduction = NULL,
  cluster_col = NULL,
  min_hits = 3,
  min_offspring = 100,
  trans_fun = NULL,
  replace_char = list(. = ":", `_` = ":"),
  value_threshold = NULL,
  p_threshold = 0.05,
  q_threshold = 0.05,
  top_n = 100,
  sort_by = c(log2_fold_enrichment = -1)
)
}
\arguments{
\item{value_threshold}{Minimum weight to include a term 
(after applying \code{truns_fun}).
 Only used when \code{reduction} is provided.}
}
\description{
Run enrichment of terms in a DAG ontology.
}
\examples{
ont <- KGExplorer::get_ontology("hp")
obj <- get_HPO()
res <- run_dag_enrich(obj, ont, reduction="pca")
}
